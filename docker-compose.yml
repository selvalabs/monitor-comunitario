services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: monitor_comunitario
      POSTGRES_USER: monitor
      POSTGRES_PASSWORD: monitor
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - monitor_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U monitor -d monitor_comunitario"]
      interval: 5s
      timeout: 5s
      retries: 20

  migrate:
    build:
      context: .
      dockerfile: Dockerfile
    command: uv run monitor-comunitario db-upgrade
    env_file:
      - .env.docker.example
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      uv run uvicorn monitor_comunitario.api.main:app
      --host 0.0.0.0
      --port 8000
    env_file:
      - .env.docker.example
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./snapshots:/app/snapshots
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: uv run monitor-comunitario worker
    env_file:
      - .env.docker.example
    volumes:
      - ./snapshots:/app/snapshots
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped

volumes:
  monitor_postgres_data:
